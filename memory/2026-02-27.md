# Memory — 2026-02-27

## Layer 9: clawmacs-core

### Features shipped

**Layer 9a — Telegram Streaming Partial Responses** (`src/telegram.lisp`)
- Added `*telegram-streaming*` (bool, default T) and `*telegram-stream-debounce-ms*` (int, default 500ms)
- `telegram-edit-message` — wraps `editMessageText`, silently swallows "message is not modified" 400s
- `%current-time-ms` — millisecond timer via `get-internal-real-time`
- `%split-telegram-text` — splits at 4096 chars, prefers newline then space boundaries
- `%run-agent-streaming` — sends `…` placeholder, debounces `editMessageText` on stream deltas, final edit + overflow chunked via `%split-telegram-text`
- `process-update` now dispatches on `*telegram-streaming*`
- `register-channel :telegram` accepts `:streaming` keyword

**Layer 9b — IRC Per-Channel Allowlists** (`src/irc.lisp`)
- New slots on `irc-connection`: `channel-policies` (alist `(chan :allowed-users list)`) and `dm-allowed-users`
- `%effective-channel-allowed` — channel-specific policy first, falls back to global
- `%effective-dm-allowed` — `dm-allowed-users` if set, else global
- `%handle-privmsg` rewritten to use per-target allowlist check
- `register-channel :irc` and `start-irc` accept `:channel-policies` and `:dm-allowed-users`

### Commit
`70e15b8` — "Layer 9a/b: Telegram streaming partial + IRC per-channel allowlists"

### Test results
261 total tests, 0 failures across Telegram (54), IRC (98), Browser (28), Cron (52), Remote API (29).

### Incident
Sub-agent timed out at 10 min (parenthesis mismatch in `telegram.lisp` line 454 — 11 closing parens instead of 10 in 10-level deep nesting). Fixed manually with Python script to handle Unicode ellipsis `…` char in search-replace. Logged as mistake #26.
